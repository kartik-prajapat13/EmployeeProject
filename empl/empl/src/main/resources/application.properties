spring.application.name=empl


# MySQL settings
spring.datasource.url=jdbc:mysql://localhost:3306/emp2
spring.datasource.username=root
spring.datasource.password=root123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect

# Automatically update the database schema - If table does not exist inside the Database
spring.jpa.hibernate.ddl-auto=update

# Show SQL queries in the console (optional)
spring.jpa.show-sql=true
